/build
/bin
/local-config.properties
/local-boot.properties
/build.properties
/.idea
*~

# Local development tools
/apache-ant*/
*.zip
/jdk-*/
hafen.jar
/.claude/*
!/.claude/CLAUDE.md

# SQLite databases (user data)
*.db
*.db-journal
client.log

# Personal alarm config (kept out of bin/ so clean builds don't wipe it; per-user, not shipped)
/AlarmSounds/settings/yourSavedConfig
